import { Page } from '../../fixtures';
import { BaseDeleteDialog } from './baseDeleteDialog';

export class DeleteEmailSyncDialog extends BaseDeleteDialog {
  constructor(page: Page) {
    super(page);
  }
}
